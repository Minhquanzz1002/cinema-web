import React from 'react';

const Sidebar = () => {
    return (
        <div className={`duration-1 fixed !z-50 flex min-h-full flex-col bg-white `}>

        </div>
    );
};

export default Sidebar;